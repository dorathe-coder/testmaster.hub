import React from "react"
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { QuizProvider } from "@/lib/quiz-context"
import { AdminProvider } from "@/lib/admin-context"
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'TestMaster Hub - Professional Quiz & Mock Test Platform',
  description: 'Ace your exams with TestMaster Hub. Practice mock tests in Math, Science, GK and more with real-time quiz interface, instant results, and detailed analytics.',
  generator: 'v0.app',
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        <AdminProvider>
          <QuizProvider>
            {children}
          </QuizProvider>
        </AdminProvider>
        <Analytics />
      </body>
    </html>
  )
}
